# KatanA Direct Markdown Fixture

This fixture verifies that Storybook direct path loading treats `.markdown`
as a Markdown text document, not as a raw file URI.

- [x] Completed task
- [/] In progress task
- [-] Held task
- [ ] Open task

> [!NOTE]
> Direct Markdown content must pass through the same preview path as `.md`.

```rust
fn main() {
    println!("katana");
}
```
