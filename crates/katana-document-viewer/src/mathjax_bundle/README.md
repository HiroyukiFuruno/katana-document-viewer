This directory contains the MathJax browser bundle used by the temporary KRR stub.

The bundle is sourced from KatanA's `vendor/mathjax_svg/js/out/index.mjs`
so HTML and native surface exports can share the same SVG output until the
`katana-render-runtime` crate replaces this stub.
