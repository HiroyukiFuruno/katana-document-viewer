## 1. Reproduction and ownership evidence

delegation-exception: `直列のクリティカルパス` / file:
`openspec/changes/post-v0-5-5-document-fidelity-regressions/handoff.md`

- [x] 1.1 Add a real data-descriptor DOCX/XLSX/PPTX fixture and record the exact local-header/central-directory behavior. delegation-exception: `直列のクリティカルパス`
- [x] 1.2 Run the supplied XLSX/PPTX corpus through KDV and record typed failures and stage-level first-frame timings. delegation-exception: `直列のクリティカルパス`
- [x] 1.3 Record child-process, cache-entry, artifact-byte, and process-count baselines for ten mixed-document open/frame/close cycles. delegation-exception: `直列のクリティカルパス`

## 2. Office ZIP compatibility

- [x] 2.1 Replace string-matched deferred-length handling with a typed local-header scan outcome. delegation-exception: `直列のクリティカルパス`
- [x] 2.2 Accept valid data-descriptor/ZIP64 packages only after central-directory, CRC, duplicate, path, relationship, and resource-limit validation. delegation-exception: `直列のクリティカルパス`
- [x] 2.3 Add regression tests proving valid variants pass and corrupt or unsafe variants remain rejected before process spawn. delegation-exception: `直列のクリティカルパス`

## 3. Interactive spreadsheet filtering

- [x] 3.1 Add neutral AutoFilter range, column, criterion, candidate, visibility, and diagnostic artifact types. delegation-exception: `直列のクリティカルパス`
- [x] 3.2 Parse bounded worksheet AutoFilter metadata for model and streaming spreadsheet backends. delegation-exception: `直列のクリティカルパス`
- [x] 3.3 Add isolated-process requests/responses for candidate extraction and filter evaluation without materializing the full sheet into host IPC. delegation-exception: `直列のクリティカルパス`
- [x] 3.4 Add KDV document commands/events and rebuild grid row visibility while preserving source row indices, selection, scroll, frozen panes, and authored hidden rows. delegation-exception: `直列のクリティカルパス`
- [x] 3.5 Add unit, protocol, session, and rendered-frame tests for apply, clear, unsupported criteria, and large-sheet limits. delegation-exception: `直列のクリティカルパス`

## 4. Performance and lifecycle

- [x] 4.1 Add `DEBUG=true`-only stage tracing for preflight, spawn, convert, decode, frame, cache, close, and drop. delegation-exception: `直列のクリティカルパス`
- [x] 4.2 Key immutable Office conversion artifacts by content, format, and process settings and prevent navigation, resize, or repeat-frame reconversion. delegation-exception: `直列のクリティカルパス`
- [x] 4.3 Bound page/grid/artifact caches by entries and bytes with deterministic eviction tests. delegation-exception: `直列のクリティカルパス`
- [x] 4.4 Make close/drop cleanup idempotent and prove process, workspace, frame, and cache live counts return to baseline across PDF, XLSX, and PPTX ten-cycle sessions. delegation-exception: `直列のクリティカルパス`
- [x] 4.5 Measure the supplied PPTX corpus, remove the dominant avoidable stage, and record the before/after first-frame result. delegation-exception: `直列のクリティカルパス`

## 5. Verification, dependency maintenance, and release

- [/] 2026-09-25 PR #50 HEAD `af00cbd` の追加P1: `core.hooksPath=.githooks`のような相対指定を絶対managed pathと文字列比較すると、managed hook自身をdelegateに設定して再帰実行する。実ファイル同一性で判定し、既存の自己委譲設定を再導入時に除去する候補を追加。独立Git fixtureで相対path・冪等再導入・既存自己委譲からの回復・外部legacy hook保持をRED→GREENで検証し、`just release-governance-check`とBash構文検査はPASS。commit/push、current-HEAD review、P1 thread reply/resolve、3OS/preflightは未完了。証跡: <https://github.com/HiroyukiFuruno/katana-document-viewer/pull/50#discussion_r4103780432>; verify: `rtk proxy just release-governance-check`。delegation-exception: `直列のクリティカルパス`。

- [/] 2026-09-25 PR #50 HEAD `18f86c9` の追加P1: merge判定後からremote branch削除までに第三者がpushした場合、無条件deleteで未検証のtipを消し得る。確認したremote tipのOIDを`--force-with-lease`へ指定する候補を実装し、独立bare Git remoteで介在push時の削除拒否と最新tip再確認後の削除を検証した。`just release-governance-check` PASS。commit/push、current-HEAD review、P1 thread reply/resolve、3OS/preflightは未完了。証跡: <https://github.com/HiroyukiFuruno/katana-document-viewer/pull/50#discussion_r4103364052>; verify: `rtk proxy just release-governance-check`。delegation-exception: `直列のクリティカルパス`。

- [/] 2026-09-25 PR #50 review追補: P1 remote cleanup は `git fetch origin <branch>` が `FETCH_HEAD` のみを更新するため、古い `origin/<branch>` でmerge判定し得る。明示 refspecでdefault/release両remote-tracking refを更新する候補を、self-test RED→GREENと`just release-governance-check`で検証。P2 保存済み XLSX filter 値の上限追加は、単独では大きいworkbookを開けるようにせず既存受理条件を変えるため、今回実装しない根拠をreview threadに返信して個別resolve済み。P1候補は未commit/未pushで、current-HEAD review/CIは再実行が必要。証跡: <https://github.com/HiroyukiFuruno/katana-document-viewer/pull/50#discussion_r4100526437>; verify: `rtk proxy just release-governance-check`。delegation-exception: `直列のクリティカルパス`。

- [/] 2026-09-25 PR #50 HEAD `40a06c1` の3OS CIはmacOS/Ubuntu/preflight PASS、Windowsのみ本文高さ期待43px/実測22pxでFAIL（run `36095674726`）。KUC #52のowner調査で、KDV interactive paragraphがsource行数をKUCへ渡さないことが原因と判明。KDV source rectの本文行数を公開KUC本文baselineへ投影する候補で14px/24px回帰と独立KatanA参照crop/exportはPASS、`just JOBS=2 check`もPASS。現候補は未commit/未push、Windows再CI・current-HEAD review・公開は未完了。閾値、reference、path/git overrideは変更しない。delegation-exception: `直列のクリティカルパス`。証跡: <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/36095674726>; verify: `rtk proxy just JOBS=2 check`。

- [/] 2026-09-25 PR #50 HEAD `84a1e57` のfinal CI: macOS PASS、Ubuntu/Windows/preflight FAIL。共通失敗はroot-offset付きdiagram drag回帰の移動先hit欠落（CI run `36086700180`、preflight `36086700287`）。KDVテストの座標取得を本番と同じKUC typography bridgeへ合わせる候補をローカル検証中。ドラッグ座標・移動量は維持し、成功するhitを探索する変更は採用しない。Windows固有の本文高さは期待43px/実測22pxで、KUC Issue #52をCI証跡付きでreopenしowner taskへ引継ぎ済み。PRをDraftへ戻した。KUC公開修正版、final-HEAD review、3OS/preflight、merge、公開、fresh consumer、後処理は未完了。閾値・reference・geometry補償・path/git overrideは変更しない。delegation-exception: `直列のクリティカルパス`。証跡: <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/36086700180>; verify: `rtk proxy cargo test -p kdv-storybook --locked storybook_window_document_diagram_drag_uses_scroll_aware_kuc_surface_with_root_offset`。

- [x] 2026-09-25 KUC `=0.3.15`候補のローカル全体ゲート: `just JOBS=2 check`、`just JOBS=2 VERSION=v0.5.6 release-check`、OpenSpec strict（release・Issue #51）がPASS。coverage functions `3674/3674`・lines `30127/30127`（100%）、semver `196/196`、data-descriptor DOCX、V8 `152.2.0`、893-file package、publish dry-runを確認した。Draft PR current-HEAD review、three-OS CI、GitHub Release/crates.io公開、fresh public consumerは別DoDとして未完了。delegation-exception: `直列のクリティカルパス`。証跡: file: `evidence/dependency-maintenance.md`; verify: `rtk proxy just JOBS=2 VERSION=v0.5.6 release-check`。
- [x] 2026-09-25 KUC `=0.3.15`候補の独立KatanA参照95点とexport-surface parity: `just storybook-score-check`がexit 0。sample/diagrams cropとexport PNG、fixture matrix 19件、surface equivalence 27件、fast/diagram-heavy parity両件を閾値・reference・path/git override変更なしで通過。delegation-exception: `直列のクリティカルパス`。証跡: file: `evidence/dependency-maintenance.md`; verify: `rtk proxy just storybook-score-check`。

- [/] 2026-09-25: 公開KUC `0.3.15`をworkspace/公開crateのexact registry依存へ更新し、lockfile checksum `cd5de2df8c926d39516f724a267d14988dd9742754d0bee3ed81753416c8df0a`を固定した。以前失敗したdeep-scroll viewport hitを含むStorybook全件は`659 pass / 0 fail / 21 expected ignored`。`just check`、strict release-check、release contract/DoD self-test、release-target、V8 singleton `152.2.0`とlocal consumer linkはPASS。PR最終HEAD review、three-OS CI、公開KDV consumer、後処理は未完了。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`; verify: `rtk proxy cargo test -p kdv-storybook --locked -- --test-threads=1`; verify: `rtk proxy python3 scripts/release/verify-v8-runtime-singleton.py`。delegation-exception: `直列のクリティカルパス`.
- [x] PR #50 HEAD `d0d8e74` review: P1 2件（pre-push delegateのstdin再送、新規branchの全未pushコミット差分）とP2 2件（送信SHAのmanifest、TOMLのoverride形式）を回帰付きで修正・pushし、各threadへ返信・resolveした。delegation-exception: `直列のクリティカルパス`。証跡: `rtk proxy just release-governance-check`; `rtk proxy just release-contract-check`; pre-push `just JOBS=2 check`。
- [/] PR #50 HEAD `d0d8e74` review のP2 2件（タグrefの除外、delegate再送self-testのorigin非依存化）を修正候補として実装し、focused self-test と release contract はPASS。push、thread reply/resolve、最終HEAD review、three-OS CIは未完了。delegation-exception: `直列のクリティカルパス`。証跡: `rtk proxy just release-governance-check`; `rtk proxy just release-contract-check`。

- [x] 5.1 Run focused ZIP, spreadsheet filter, performance, and lifecycle regression suites. delegation-exception: `直列のクリティカルパス`
- [x] 5.2 Update compatible direct/transitive dependencies and lockfile, including public KRR `0.4.20` with caret-compatible semver (not an exact pin), without lowering lint, coverage, security, score, or acceptance gates. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `evidence/dependency-maintenance.md`.
  - [x] KRR and every non-KUC dependency are updated or rechecked against the registry; the lockfile updates compatible `libredox`、`rust_decimal`、`smallvec` and needs no further compatible update. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just update`
  - [x] KDV manifest and release-contract checks pin registry-only `katana-ui-core` `0.3.6` with no path/git override; KUC `v0.3.6` tag, GitHub Release, crates.io publication, and a fresh exact registry consumer build were verified. `just update` refreshed all compatible non-KUC dependencies without lowering a gate. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just update`
  - [x] 2026-09-06にKUC `v0.3.7`のtag、GitHub Release、crates.io artifactを確認し、KDVはexact registry `=0.3.7`へ更新した。manifest/lockfileのsource+checksum、release contract、document-surface boundary、DoD self-test、V8 singleton、依存契約・score gate契約テスト、format、AST lint、strict clippyを通過し、path/git overrideやV8重複を導入していない。公開KDV consumerとKatanA acceptanceは別DoDとして残す。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`.
  - [x] 2026-09-07に公式`office2pdf v0.6.8` tag `e01828a5f07b52bebf4574859efaca4554b134d9`とcrates.io `0.6.8`を確認し、PPTX段落間隔、CJK fallback、malformed raster omission、header-rowspan preservationの4 commitおよび後者2件の回帰test sourceが公開artifactへ収録されたことを確認した。KDVをexact registry `office2pdf =0.6.8`へ戻し、manifest/lockfile checksum、release contract、supply-chain closure、PPTX段落/CJK、実文書、strict coverageを最新HEADで再検証した。three-OS CI、公開KDV consumer、KatanA adoptionは5.3〜5.5の別DoDとして残す。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`.
  - [x] The former development-only KUC Storybook Git/tag dependency was replaced by the public registry package. Registry Storybook smoke passed with `143/30/54/121` tests against KUC `0.3.6`; no final-release Git/tag override remains. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just storybook-kuc-smoke`
  - [x] The release verifier accepts every registry KRR resolution compatible with `^0.4.20` and rejects `0.5.0`; the V8 singleton/link checks passed with one resolved V8 runtime. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy python3 scripts/release/verify-v8-runtime-singleton.py`
  - [x] 2026-09-13の再監査で`rtk proxy just update`を実行し、直接依存`katana-markdown-model 0.2.1 -> 0.2.2`と34件の互換lockfile更新を採用した。`cargo outdated --workspace`の残件はKDV直接依存ではない`embedded-io`と対象platform限定・解決系統差の項目だけで、KUC/KRRの次公開版を除く採用可能な更新はない。更新後の通常full gateはexit `0`で完走し、library `1903 passed / 0 failed / 1 ignored`、Storybook `616 passed / 0 failed / 21 ignored`、data-descriptor DOCX、Office profiling、V8 singleton `152.2.0`、subagent harnessを通過した。証跡: `rtk proxy just update`; `rtk proxy just outdated`; `rtk proxy cargo tree -d --locked`; `rtk proxy python3 scripts/release/verify-v8-runtime-singleton.py`; `rtk proxy just JOBS=2 check`. delegation-exception: `直列のクリティカルパス`.
  - [/] KatanA公開graphでKDV `0.5.5`の非exact KUC依存が`0.3.10`を取得し、`GridCellAppearance.borders`欠落のE0063になる組合せをrelease blockerとして固定する。KDV `0.5.6`はworkspaceと公開crateの両方でKUC `=0.3.11`をexact registry依存にし、裸の`0.3.11`など将来の破壊的0.x minorを拾う指定をrelease verifierで拒否する。現行候補は`borders: cell_borders(style.borders)`と公開grid modelのborder回帰を持ち、KUC `=0.3.10`でcompile済み。KUC `0.3.11`公開後にlockfile checksum、focused border/dependency tests、workspace check、fresh registry consumer compileを再実行して完了する。証跡: file: `crates/katana-document-viewer/src/document_surface/spreadsheet_grid_mapping.rs`; file: `crates/katana-document-viewer/src/document_surface/spreadsheet_grid_mapping_tests.rs`; file: `scripts/release/verify-release-contract.py`; verify: `rtk proxy cargo test -p katana-document-viewer viewer_manifest_is_backend_neutral_and_uses_registry_kuc --locked`; verify: `rtk proxy cargo test -p katana-document-viewer spreadsheet_border_sides_project_to_public_kuc_grid_model --locked`; verify: `rtk proxy python3 scripts/release/verify-release-contract.py --self-test`. delegation-exception: `直列のクリティカルパス`.
    - [x] KUC側の最新isolated consumer handoff 46パス（45 modified + 1 new）をread-only再監査し、全probe SHA-256一致、owner基底一致30・owner独自差分13・既統合2・新規1へ分類した。追加3パスはvalid encoded SVGの測定済み162pxをnative text 21pxへ切らない`builder_media_height` helperと、公開後`=0.3.11`へ更新する2つの依存source guardである。`storybook_score_gate_tests.rs`に混在する旧`0.3.7`期待値へ戻すhunkは不採用とする。KDV coreだけを公開KUC `0.3.10`へ先行適用する互換probeはnative text metrics差により4件失敗し、0.3.11との一体統合が必要と確定したため試行hunkを除去した。復元後のowner差分はlibrary `1903 passed / 0 failed / 1 ignored`、format、diff checkが再び成功している。KUC側の最終consumer session `31195`はall-target Clippy、library `1907 passed / 0 failed / 1 ignored`、Storybook `645 passed / 0 failed / 21 ignored`をすべてexit `0`で完走し、前回8秒超過したsequence diagramも同一制限のfull run内で成功した。KUC 3317-file snapshotとconsumer 46 hashesにdriftはない。公開後も閾値やtimeoutを変えず、KDV ownerのregistry-only full suiteをゼロ失敗で完走させる。handoffを丸ごと上書きせず、責務/hunk単位で現行差分へ統合する。証跡: file: `/Users/hiroyuki_furuno/works/private/katana-ui-core/tmp/v0311-release/consumer-complete-handoff.md`; file: `/Users/hiroyuki_furuno/works/private/katana-ui-core/tmp/v0311-release/consumer-complete-source-hashes.json`; file: `/Users/hiroyuki_furuno/works/private/katana-ui-core/tmp/v0311-release/consumer-final-3-result.md`; verify: `rtk proxy cargo test -p katana-document-viewer --lib --locked`; verify: `rtk proxy cargo fmt --check`; verify: `rtk proxy git diff --check`. delegation-exception: `直列のクリティカルパス`.
    - [x] KUC v0.3.11最終gate14はexit `0`で、macOS `4535 tests / 129 suites`、Linux `128 binaries / 4545 passed / 0 failed / 0 ignored`、strict LCOV、package、publish dry-run、scope、未公開確認を通過した。開始時の3317-file snapshotは終了後drift `0`。consumer final-3のClippy、library `1907/0/1`、Storybook `645/0/21`も維持されている。PR #58は候補`ec0a4b04555ddd1baa45cb5dc919fa10394bae1b`を通常merge `742ebd6cb877667422c1723cb56b298c294272cf`し、両commitのtree `0ba2dc54eeed9a9c78058530f6ec65f98f5633c6`一致を確認した。Release run `34775455520`は進行中で、GitHub Release、crates.io SHA/checksumを未完了の公開境界として残す。証跡: command: `rtk proxy gh pr view 58 --repo HiroyukiFuruno/katana-ui-core --json state,mergedAt,mergeCommit,headRefOid`; command: `rtk proxy gh run view 34775455520 --repo HiroyukiFuruno/katana-ui-core --json status,conclusion,headSha`; PR: <https://github.com/HiroyukiFuruno/katana-ui-core/pull/58>; Actions: <https://github.com/HiroyukiFuruno/katana-ui-core/actions/runs/34775455520>. delegation-exception: `直列のクリティカルパス`.
    - [x] 46-path handoffをhunk単位で統合した。分類はbaseline-owner 30、owner-diverged 13、already-probe 2、新規1で、13競合パスの採否、旧`0.3.7`期待値、probe専用geometry診断、probe path/patch/generated artifact、KUC P2 #59–62 consumer補償の不採用を固定した。公開前に依存guard以外の44 source/test pathを6責務batchでowner差分へ統合し、core library `1907 passed / 0 failed / 1 ignored`、format、AST lint、strict library Clippy、diff check、task harnessを通過した。独立self-reviewはP0/P1なし、新規P2なしを確認した。KUC公開後はKRR `0.4.19`を保持してKUC `=0.3.11`だけをregistry更新し、KUC #52のcanonical両fixture `95`、export、hover、full Storybookを先に完了する。KRR `0.4.20`公開待ちはこのKUC受入の前提にせず、KDV全releaseの後続gateとする。各上流の公開証跡前は当該manifest/lockfileとexact registry source guardだけを更新しない。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/kuc-v0.3.11-handoff-integration.md`; agent: `01a09c47-5daf-7d63-97e0-d7cffff0dbbb` / model: `gpt-5.6-terra` / reasoning: `high`; verify: `rtk proxy cargo test -p katana-document-viewer --lib --locked`; verify: `rtk proxy cargo fmt --check`; verify: `rtk proxy just ast-lint`; verify: `rtk proxy cargo clippy -p katana-document-viewer --lib --tests --locked -- -D warnings -D clippy::unwrap_used -D clippy::expect_used -D clippy::todo -D clippy::unimplemented -D clippy::dbg_macro -D clippy::panic -D clippy::wildcard_imports`; verify: `rtk proxy git diff --check`; verify: `rtk proxy bash scripts/check-subagent-spark-harness.sh`. delegation-exception: `直列のクリティカルパス`.
    - [/] 2026-09-19にKUC `v0.3.11`のmerge `742ebd6cb877667422c1723cb56b298c294272cf`、GitHub Release、crates.io checksum `45eacea577b7a36cbdc7aecbcabbe56ebce11239ae9e6d36f4f244fdabbf6557`を確認し、KDV両manifestをexact registry `=0.3.11`へ更新した。KRR `0.4.19`とV8 `152.2.0`単一解決を維持し、path/git overrideなし、focused border/dependency、library `1907 passed / 0 failed / 1 ignored`、format、AST lint、strict library Clippy、document-surface boundary、subagent harness、export/hoverを含むscore以外のStorybook `627 passed / 0 failed / 21 ignored`を確認した。既存の独立KatanA参照ではsample `52/95`、diagrams `89/95`で、旧KUC probeの95点参照SHA（sample `7183c95d24e7910dfb088f837cb8b37c8faa98c4f01ad51441c1287009ee91dc`、diagrams `31fc692ccbe6a803242768ae3a5fd8df7346f8ec0307fb353dd09ea54d99ecda`）が追跡参照SHA（sample `88745146437b30cf44bfe3e9771fc8d528d8dd6aab4cf050fd9ebc1b3920ed52`、diagrams `890c69378bdadd78c1b11f0558dadf9988483fb2662a2cd24792ec48fadb66ef`）と異なるため、旧probeの95点を受入証跡にはしない。KUC #52の公開依存受入は完了し、最終95点はclean KDV候補を採用した独立KatanA artifactのprovenance検証待ち。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/kuc-v0.3.11-handoff-integration.md`; issue comment: <https://github.com/HiroyukiFuruno/katana-ui-core/issues/52#issuecomment-5740630331>. delegation-exception: `直列のクリティカルパス`.
    - [/] 2026-09-20: PR #50のWindows Storybook公開gateで、KatanA sample本文の先頭説明が一行化し、line target height `43.0`に対して`22.0`となる再現を固定した。KDV側で期待値・viewport・geometryを補償変更せず、KUC #52のcandidate font-chain解決を公開patchとしてregistryに出した後、そのexact版だけを更新する。KUC PR #67のfile-length gate、fresh registry consumer、KDVのthree-OS CI・full release gateを順に通して完了する。path/git override、score/reference閾値緩和は禁止。証跡: KDV CI <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/35481786716>; KUC Issue <https://github.com/HiroyukiFuruno/katana-ui-core/issues/52>; KUC PR <https://github.com/HiroyukiFuruno/katana-ui-core/pull/67>. delegation-exception: `直列のクリティカルパス`.
    - [/] 2026-09-21: PR #50 `dd4c2aa`のrelease-preflightはKUCとは独立にRust build出力の容量枯渇で失敗した。acceptance artifactを`RUNNER_TEMP`へ退避してからdefault targetと`target/llvm-cov-target`を明示的に掃除し、同一の`release-check`をクリーンtargetで実行する。`release-verify`内でもdefault targetの`check`完了後にcleanし、coverage完了後にcoverage targetをcleanしてpackage/publish dry-runと同居させない。失敗時は退避先と通常targetの両方をuploadする。release gate、coverage、artifact収集を下げず、公開KUC patchを取り込んだ最終HEADで再実行する。証跡: command: `rtk proxy gh run view 35511353482 --repo HiroyukiFuruno/katana-document-viewer --log-failed`; command: `rtk proxy gh run view 35537459549 --repo HiroyukiFuruno/katana-document-viewer --log-failed`; command: `rtk proxy gh run view 35547444850 --repo HiroyukiFuruno/katana-document-viewer --log-failed`; <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/35511353482>; <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/35537459549>; <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/35547444850>.
    - [/] 2026-09-23: 公開KUC `0.3.12`をexact registryでworkspace/公開crate/lockfileへ取り込んだ。checksumは`d976c727f10f5b46f9bc1b538ab8b6c4d3f8e007554c4edcc893061f1e742d17`。release contract、V8 singleton、document-surface boundary、focused manifest testはPASS。Storybook registry consumerはaccordion viewport hitの単体再現失敗（658 pass / 1 fail）、直接diagram parityは従来同様90/95であり、Windows typography CIとfull release gateは未完了。KUC #52へ再現を連携し、KDV側では閾値・参照・path/git overrideを変えない。証跡: file: `evidence/kuc-v0.3.12-integration.md`。
    - [/] 2026-09-23: KUC PR #69はKUC単体再現不能としてclose・未merge。PR #70のv0.3.13候補はemoji font/runner更新でviewport hit修正を含まない。KDV `sample_basic.md`のdeep scroll `5901.5`/root offset `0`/content height `28596`で、同一KUC tree/areaの従来`viewport_host_action_hits`はaccordion 1件、新`viewport_interaction_hits`は0件（focused test exit 101）。KUC 0.3.12の新経路は0.3.11のDocument clipからViewport clipへ変更され、増分収集の早期停止を通る。既存の実window accordion testは同位置のclickで閉→開・body region 64 pixel超の変化を確認しPASS。API間差異と表示経路の証跡を#52へ連携済み。KDV owner-layerなら回帰修正し、KUC owner-layerなら最小再現に基づく公開修正版を待つ。PR #70公開をhit修正の代替条件として扱わない。閾値・参照・geometry補償・path/git overrideは禁止。証跡: command: `rtk proxy cargo test -p kdv-storybook accordion_click_accepts_kuc_viewport_surface_hit --locked -- --nocapture --test-threads=1`; command: `rtk proxy cargo test -p kdv-storybook storybook_window_closed_accordion_click_opens_body_pixels --locked -- --nocapture --test-threads=1`; issue: <https://github.com/HiroyukiFuruno/katana-ui-core/issues/52#issuecomment-5794768341>。
    - [/] 2026-09-24: KUC PR #70 merge、GitHub Releaseとcrates.io公開を確認し、両manifestとlockfileをexact registry `=0.3.13`（checksum `8661e359c8af3f6f9e2249849dd0b4a94a2e6401d4eb91ded9fb36bf44a33b55`）へ更新。公開crateのviewport hit sourceは0.3.12と同一で、focused testは新hit 0件/従来hit 1件のまま失敗。Storybook全件は658 pass/1 fail/21 ignoredで新規失敗なし。KUC #52とowner taskへ再引継ぎ済み。KDV core 1919 pass、依存境界、strict lint、fmt、AST、semver196/196、release contract、V8 singletonとconsumer linkはPASS。Windows typography、strict release-check、最終HEAD review/CIは未完了。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`; issue: <https://github.com/HiroyukiFuruno/katana-ui-core/issues/52#issuecomment-5805105732>。
- [/] FB-2026-08-29-001: 全体のKUC blockerでも、KDV内で独立して進められる品質ゲート、Draft PR、CI準備を停止しない。後続の状況確認はこの実装指示を置換しないため、報告前に未完了項目を実行可能・外部入力待ち・公開待ちへ分け、実行可能な項目を継続する。グローバルポリシーの表記差は`reasoning`の`medium`基準を意味で検証し、回帰テストで維持する。delegation-exception: `直列のクリティカルパス` / 証跡: file: `scripts/check-subagent-spark-harness-policy-tests.sh`.
<!-- subagent-spark-harness-strict-start -->
- [ ] 5.3 Run strict OpenSpec validation, `just check`, 100% coverage, boundary, scorecard, package, publish dry-run, and three-OS CI gates. delegation-exception: `直列のクリティカルパス`
  - [/] 2026-09-25 PR #50 current-HEAD review追加指摘: post-release cleanupはGitHubが先にremote branchを削除した場合を成功済みcleanupとして扱い、`ls-remote`の終了2だけを不存在と判定する。失敗診断で使う`sys`をimportし、release不存在のstderrも回帰固定した。`just release-governance-check`と`just release-contract-check`はPASS。修正commit/push、P1/P2 threadへの個別reply/resolve、新HEAD review・three-OS CIは未完了。delegation-exception: `直列のクリティカルパス`。証跡: file: `scripts/release/post-release-cleanup.py`; verify: `rtk proxy just release-governance-check`; verify: `rtk proxy just release-contract-check`。
  - [/] 2026-09-23局所再検証: workspace strict Clippy、AST lint、fmt、OpenSpec strict（release/Issue #51）、semver `196/196`、library `1918 pass / 0 fail / 1 ignored`、Issue #51 fast/diagram-heavy parityがPASS。Storybook全件は`658 pass / 1 fail / 21 ignored`で公開KUC 0.3.12のdeep-scroll viewport hitのみ失敗。full `release-check`、coverage、Draft PR最終HEAD review、three-OS CIは未完了であり、上流修正・registry-only再検証後に行う。delegation-exception: `直列のクリティカルパス`。証跡: command: `rtk proxy just JOBS=2 lint`; command: `rtk proxy just ast-lint`; command: `rtk proxy just semver-check`; command: `rtk proxy cargo test -p katana-document-viewer --lib --locked -- --test-threads=2`; command: `rtk proxy cargo test -p kdv-storybook --locked -- --test-threads=1`。
  - [/] 2026-09-23: 前回clean後のStorybook単体再ビルドはRust依存compile中に`No space left on device`で停止。共有volume空き117 MiB、当worktreeの`target/debug`は2.8 GiB、KUC/KatanA/KDV他checkoutのtargetはそれぞれ約70/63/15 GiB。ソースのテスト失敗ではなく容量gateであり、他taskの生成物には触れずKUC/KLE owner taskへ容量調整を連絡済み。その後空きは約73 GiBに回復し再ビルドできたが、KUC 0.3.12の深いscrollで従来hit 1件に対し新interaction hit 0件を再現した。release contract self-testと`git diff --check`はPASS。原因切り分け後にfull gateを再実行する。閾値・検証範囲は下げない。delegation-exception: `直列のクリティカルパス`。証跡: command: `rtk proxy cargo test -p kdv-storybook accordion_click_accepts_kuc_viewport_surface_hit --locked -- --nocapture --test-threads=1`; issue: <https://github.com/HiroyukiFuruno/katana-ui-core/issues/52#issuecomment-5794750075>。
  - [/] 2026-09-23 PR #50未解決P1のRelease容量枯渇を候補修正。acceptance生成後、preview-crop診断をRUNNER_TEMPへ保全し、通常/coverage targetをcleanしてからrelease-verifyを実行。失敗uploadは元/退避両方を対象にした。contract self-testで欠落/順序をRED確認後、実workflowはPASS。未コミット・未pushでthread reply/resolveとthree-OS CIは未完了。delegation-exception: `直列のクリティカルパス`。証跡: file: `.github/workflows/release.yml`; file: `scripts/release/verify-release-contract.py`。
- [x] 2026-09-04の候補作業ツリーで`rtk proxy just VERSION=v0.5.6 release-check`はexit `0`。strict coverageはfunctions `3531/3531`・lines `29061/29061`（各100%）、data-descriptor DOCX worker、V8 singleton、package build、`cargo publish --dry-run`、未公開registry確認まで通過した。KatanA候補referenceの更新後にはPR最終HEADで同gateを再実行する。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just VERSION=v0.5.6 release-check`。delegation-exception: `直列のクリティカルパス`
  - [x] 2026-09-05の候補差分で`rtk proxy just VERSION=v0.5.6 release-check`はexit `0`。Retina図表スクロール境界の回帰、strict coverage functions `3532/3532`・lines `29064/29064`（各100%）、data-descriptor DOCX worker、V8 singleton、package build、`cargo publish --dry-run`、未公開registry確認まで通過した。three-OS CIとPR最終HEADのfresh reviewは未完了の公開gateとして残す。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just VERSION=v0.5.6 release-check`。delegation-exception: `直列のクリティカルパス`
  - [ ] three-OS CI は、同一差分を commit/push した Draft PR 上で実行する公開 gate。
- [/] KRR 0.4.19 のrenderer更新でKatanA canonical score artifactが陳腐化したため、95点gateの緩和またはKDV自己比較をせず、KDV v0.5.6候補を実際に採用したKatanAから独立生成したexport PNGとpreview cropを取得する。generator、fixture SHA-256、KDV/KRR解決版、出力SHA-256・寸法を記録して追跡済みreferenceを更新し、focused scoreとrelease gateを再実行する。KDV 0.5.5 / KRR 0.4.19で生成されたexport PNG（`2dddc1c54b07e3f1132ea0e269d2106e074b3864b1faafb734110939b3afe66d`、1280x19067）は陳腐化の診断証跡であり、v0.5.6の最終referenceには使わない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`. delegation-exception: `直列のクリティカルパス`
  - [/] 2026-09-06: clean package SHA-256 `3196397aec631fc31524d70647c7fdc12479be16698f955a09deb48bdca07020`のみをloopback sparse registryから解決した使い捨てKatanA cloneで、KDV `0.5.6`、KRR `0.4.19`、KUC `0.3.7`、V8 `152.2.0`の単一解決を確認した。実機runnerがfixture `sample.md` / `sample_diagrams.md`からexportとfull screenshotを生成したが、`sample_diagrams.md`の図表本体が未描画となり、既存の客観scoreは`3/95`だった。既存physical crop `2374x4450+64+202`をBox filterで`1280x2400`へ正規化して診断したが、追跡referenceは直ちに復元済みである。KatanA host output SHA-256はexport `88a3342dadc95e46fa1db5e5c942017670e399d8199eca63b57ff73622675a1c`、sample full `3ec7ab6e530ec68acce6655c881af858f6bd3a36a5437be52b2a0e5c970849de`、diagrams full `11cced597dacbe2e641fd850b49d6744721ec2e3dfd3e41fed175994d63f4321`。実機frame描画の回帰を修正してfocused scoreとrelease gateを再実行するまで未完了。証跡: issue: <https://github.com/HiroyukiFuruno/KatanA/issues/339#issuecomment-5556255336>; file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`. delegation-exception: `直列のクリティカルパス`
  - [/] 2026-09-13: KUC公開候補を待つ間もKDV内の独立作業を継続し、interactive Text/Listのnatural height、canonical body-row由来のMarkdown spacing、interactive content ROIのroot top padding `0`を実装した。export surfaceの固定height/paddingは維持し、adapter `3/3`、hover matrix `4/4`、surface parity `9 pass / 2 expected ignored`をすべてexit `0`で確認した。公開KUC `0.3.10`でfocused scoreを再測定するとsample `55/95`、diagrams `90/95`でfailし、export scoreはpassした。KUC正式版のregistry-only採用、独立KatanA referenceによる両fixture `95`点、最終release gateは未完了。証跡: `rtk proxy cargo test -p kdv-storybook --locked storybook_score_visual_uses_katana_ -- --nocapture --test-threads=1`. delegation-exception: `直列のクリティカルパス`.
    - [x] KUC `0.3.10`採用後も`0.3.7`を要求していたdocument-surface boundary、release DoD、release contractの検査を現manifest/lock契約へ揃えた。証跡: `rtk proxy just document-surface-boundary-check`; `rtk proxy python3 scripts/release/assert-viewer-recovery-dod.py --self-test`; `rtk proxy python3 scripts/release/verify-release-contract.py --self-test`. delegation-exception: `直列のクリティカルパス`.
    - [x] `rtk proxy just JOBS=2 check`の再実行でlibrary `1903 pass / 1 expected ignored`、Storybook `616 pass / 21 expected ignored`、追加scroll test `1 pass`、data-descriptor fixture、Office profiling/fidelity、V8 singleton `152.2.0`まで成功した。先行実行で一度だけtimeoutした10-cycle resource lifecycleも単独 `42.09s`、再実行内 `17.55s`で成功した。bottom target自然高、HTML alignment、sample description行高を含む既知5件は解消済みで、最後に残った本tasks行の証跡形式を修正後、subagent harnessとdiff checkもexit `0`で確認した。証跡: `rtk proxy just JOBS=2 check`; `rtk proxy bash scripts/check-subagent-spark-harness.sh`; `rtk proxy git diff --check`. delegation-exception: `直列のクリティカルパス`.
    - [x] 未コミット差分の独立自己レビューでP0なしを確認した。P1は上流公開後の最終gate・公開・downstream adoptionだけで、P2はhost生成コスト、test helperの1px underline除外、natural-height listの追加ケースだった。いずれも現行必須failureを再現せず、ユーザー指示に従ってv0.5.6へ追加実装しない。証跡: agent: `01a09aa8-826f-7833-b6bc-e770856ce40a` / model: `gpt-5.3-codex-spark` / reasoning: `medium` / file: `tools/kdv-storybook/src/preview_theme_bridge.rs` / file: `tools/kdv-storybook/src/frame_html_alignment_tests.rs` / file: `tools/kdv-storybook/src/document_viewer/node_factory.rs` / command: `multi_agent_v1.spawn_agent` / verify: `rtk git diff --check` / close: `multi_agent_v1.close_agent`.
- [/] Preflight candidate-registry lane: cleanなKDV release commitを`cargo package --locked`し、SHA-256を持つ`.crate`だけを127.0.0.1 loopback sparse registryで配信する。使い捨てKatanA cloneはexact `=0.5.6`をそのregistryから解決し、KDV依存へのpath/git/patch overrideなし・KDV/KRR/V8単一解決をmetadata/lockfileで証明したうえでhost export/cropを生成する。このlaneはpreflight循環を断つ候補証跡に限り、公開後にはfresh crates.io KDV artifactのchecksum比較、registry-only consumer link、KatanA packaged acceptanceを別途必須とする。fixture/request/crop/normalization/PNG/hash/log/versionを記録し、推測でreferenceを更新しない。Issue: <https://github.com/HiroyukiFuruno/katana/issues/339>. delegation-exception: `直列のクリティカルパス`.
  - [x] loopback sparse registry builderは、128 MiB crate、50,000 member、512 MiB unpacked、1 MiB `Cargo.toml`の上限、symlink/path/git/registry override拒否、型付きCargo metadata、同一filesystemのtemporary→rename publishを実装し、失敗時にpartial outputを残さない。malformed gzip、manifest欠落、invalid TOML、unsafe archive entry、型不正、非registry依存をself-testで拒否する。証跡: command: `rtk proxy python3 -B scripts/feasibility/build_candidate_registry.py --self-test`; file: `scripts/feasibility/build_candidate_registry.py`. delegation-exception: `直列のクリティカルパス`.
  - [/] clean候補commit `29c591a4475c32164b994412bab388bd25e54221`をpackageし、`.crate` SHA-256 `804a6abd093db77c8e7d57eb7a99b96d7f23af7437cf137f98ac91bb98a53ba6`（881 files、14.9 MiB、圧縮7.3 MiB）のclean unpack compileを完了した。loopback sparse registry rootは`target/candidate-registry/29c591a`。KatanA owner taskへcommit/package/checksum/registry rootを渡し、exact `=0.5.6`、KUC `0.3.11`、KRR `0.4.19`、V8 `152.2.0`、KDV path/git/patch overrideなしでcanonical export/crop生成を依頼済み。artifact provenance・SHA・寸法・crop座標と95点再検証は未完了。証跡: file: `target/candidate-registry/29c591a/candidate-registry-evidence.json`; verify: `rtk proxy cargo package -p katana-document-viewer --locked`. delegation-exception: `直列のクリティカルパス`.
  - [x] KRR `0.4.20`公開後の候補commit `1b0ad91875a5a7062972614f6272fc118c56bc3b`をpackageし、`.crate` SHA-256 `3ea7eb4dd58409b9ac37345036186c38043e3d2cf3db7e711eba9261cd58659d`（881 files、14.9 MiB、圧縮7.3 MiB）を`target/candidate-registry/1b0ad91`へ配置した。KUC `=0.3.11`、KRR `^0.4.20`、V8 `152.2.0`単一解決、path/git/patchなしを確認した。旧`29c591a` artifactはKRR `0.4.19`のため最終証跡に使わない。KRR更新直後は陳腐化した2 referenceでfull Storybookが7件失敗したが、独立KatanA artifactへ更新して閾値を維持したまま解消した。HTML tableはKRR出力済みMarkdown行を一つのtable blockへ保持するowner-layer回帰を追加し、focused surface parityは95点条件を通過した。証跡: file: `target/candidate-registry/1b0ad91/candidate-registry-evidence.json`; verify: `rtk proxy cargo package -p katana-document-viewer --locked --allow-dirty --no-verify`; verify: `rtk proxy cargo test -p kdv-storybook --locked frame::test_modules::surface_parity_tests::storybook_frame_matches_export_surface_for_katana_viewer -- --ignored --exact --test-threads=1 --nocapture`. delegation-exception: `直列のクリティカルパス`.
    - [x] KatanA commit `af55949c4ee7ddad414ed93fa360a67e9464acf4`の独立runnerで、同candidateからcanonical exportと両cropを再生成した。fixture、inventory、runner、crop座標、Box正規化、出力SHAと寸法を固定し、追跡reference更新後の`just storybook-score-check`はlibrary `1910 pass / 1 expected ignored`、Storybook `652 pass / 21 expected ignored`、両fixtureとexportの95点条件を含む全gateでexit `0`。閾値緩和、KDV自己比較、path/git/patch overrideはない。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`; verify: `rtk proxy just storybook-score-check`. delegation-exception: `直列のクリティカルパス`.
- [ ] 5.4 Publish the KDV patch release and verify the GitHub Release and crates.io artifact. delegation-exception: `直列のクリティカルパス`
- [/] 2026-09-20 dependency maintenance: registry-only `office2pdf` was updated from `=0.6.8` to `=0.7.0` (including the locked Typst 0.15 dependency graph). The workspace MSRV is 1.95.0, which covers the upstream 1.92 requirement. `cargo check --workspace --locked`, the 11-case Office worker contract (including the exact KatanA data-descriptor DOCX frame), and the 15-case bounded ZIP preflight contract pass. The final full release gates, fresh review, and publication evidence remain required after the public KUC patch is adopted. 証跡: verify: `rtk proxy cargo check --workspace --locked`; verify: `rtk proxy cargo test -p katana-document-viewer --test multi_format_office_worker_contract --locked`; verify: `rtk proxy cargo test -p katana-document-viewer --test multi_format_office_preflight_contract --locked`. delegation-exception: `直列のクリティカルパス`
- [/] 2026-09-20 Windows regression recovery: the failed CI target `sample_top_description_target_uses_rendered_line_height` reported KatanA's fixed 43px description band as 22px. KatanA loads the first usable font from its ordered Windows candidates; KDV had diverged in `f4f8621` by delegating only Windows to `PlatformTextFaceSelection::System`. The ordered Yu Gothic/Meiryo candidates remain, and KDV now uses `FirstCandidate` on every platform. No viewport, expected geometry, score, or reference threshold changed. `verify-release-contract.py` and its self-test now require the selected registry `office2pdf 0.7.0` lock entry, preventing a release contract from silently retaining the prior dependency. Focused typography/description tests, `just JOBS=2 check`, and `just VERSION=v0.5.6 release-check` pass; V8 remains the single registry `152.2.0`. The final fresh review, three-OS CI, and public artifact checks remain required. 証跡: KDV CI <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/35481786716>; verify: `rtk proxy cargo test -p kdv-storybook --locked bridge_preserves_katana_platform_font_resolution_contract -- --test-threads=1`; verify: `rtk proxy cargo test -p kdv-storybook --locked sample_top_description_target_uses_rendered_line_height -- --test-threads=1`; verify: `rtk proxy just JOBS=2 check`; verify: `rtk proxy just VERSION=v0.5.6 release-check`. delegation-exception: `直列のクリティカルパス`
- [ ] 5.5 Update KatanA to the exact published registry version with no path/git override and rerun the supplied-file and ten-cycle packaged acceptance suites. delegation-exception: `直列のクリティカルパス`

## 6. v0.5.6 issue coverage added after owner-layer audit

- [x] 6.1 Issue #46: add the exact KatanA data-descriptor DOCX SHA-256 fixture, verify all 20 local headers defer CRC/sizes, and prove the isolated worker generates a frame without lowering archive limits. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/46>.
  証跡: `evidence/issue-coverage.md`.
- [x] 6.2 Issue #47: cover typed string, numeric, blank, multiple-value, and clear AutoFilter behavior from a real XLSX-derived fixture while preserving grid frame visibility. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>.
  証跡: `evidence/issue-coverage.md`.
- [/] PR #50 P1 follow-up: persist supported saved AutoFilter criteria at open, distinguish their hidden rows from authored hidden rows when clearing, stream filter chunks without retaining the full grid, return current materialization responses independently of bounded cache admission, and tokenize HTML spans at inline-style boundaries. Run focused regressions, a full local release gate, reply/resolve every P1 thread, and obtain a fresh-head review before Ready. Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. 証跡: `rtk proxy just coverage`; `rtk proxy just JOBS=2 check`. delegation-exception: `直列のクリティカルパス`.
  証跡: `rtk proxy just coverage`; `rtk proxy just JOBS=2 check`.
- [/] PR #50 fresh-head P2 follow-up: apply/clear後のAutoFilter criteriaをartifact/frameへ同期し、候補値をworker response byte上限内でtruncateし、direct HTML CSS attribute名を完全一致で判定し、SpreadsheetCellArtifactが所有する全heap領域をcache容量へ算入する。各回帰・full release gate・4 threadのreply/resolve・さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. 証跡: `rtk proxy cargo test -p katana-document-viewer document_session_spreadsheet --lib`; `rtk proxy cargo test -p katana-document-viewer spreadsheet_cell_cache --lib`; `rtk proxy cargo test -p katana-document-viewer direct_html_css --lib`; `rtk proxy just VERSION=v0.5.6 release-check`. delegation-exception: `直列のクリティカルパス`.
  証跡: `rtk proxy cargo test -p katana-document-viewer document_session_spreadsheet --lib`; `rtk proxy cargo test -p katana-document-viewer spreadsheet_cell_cache --lib`; `rtk proxy cargo test -p katana-document-viewer direct_html_css --lib`; `rtk proxy just VERSION=v0.5.6 release-check`.
- [/] PR #50 fresh-head P1/P2 review follow-up: Viewer node-planでrich HTMLのstyle境界と`br`境界を保持し、同一行の全HTML要素へCSSを独立適用し、DEBUG Office/XLSX trace全行をsession/source識別子で関連付ける。各回帰・full release gate・4 threadのreply/resolve・さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib`; `rtk proxy cargo clippy -p katana-document-viewer --lib --tests -- -D warnings`.
  証跡: `rtk proxy cargo test -p katana-document-viewer --lib`; `rtk proxy cargo clippy -p katana-document-viewer --lib --tests -- -D warnings`; PR #50 review threads.
- [/] PR #50 fresh-head P1/P2 follow-up（`050395a`後）: streaming backendの候補抽出・filter評価は各operationでworksheet ZIP entryを一回だけ逐次走査し、bounded chunkと既存filter semanticsを保持する。候補responseは後続の別request IDの`ApplyFilter` JSON（改行込み）でも512 KiB request上限内に収める。large streaming XLSXの一回走査/候補/適用/clearと最大request ID境界を回帰化し、full release gate、2 threadのreply/resolve、さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib spreadsheet_engine::filter_tests`; `rtk proxy cargo test -p katana-document-viewer --lib candidate_truncation`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 review threads.
- [/] PR #50 CI corrective: Ubuntu/Windows workspace testでbrowser sessionの初期frame生成が失敗した根因を修正し、初期frame・owner停止後closeの実経路を回帰で固定する。timeout延長・test無効化・品質ゲート低下は禁止。根因はKRRの並行V8 cold-start契約であり、owner issue <https://github.com/HiroyukiFuruno/katana-render-runtime/issues/76> の公開registry修正版採用後に対象CIとfull release gateを最終HEADで通すまでReady化しない。delegation-exception: `書き込み範囲を明示できない`. 証跡: GitHub Actions run `33840894570` / jobs `100922795136`, `100922794777`; issue: <https://github.com/HiroyukiFuruno/katana-render-runtime/issues/76>.
  証跡: GitHub Actions run `33840894570` / jobs `100922795136`, `100922794777`; KRR issue <https://github.com/HiroyukiFuruno/katana-render-runtime/issues/76>.
- [/] PR #50 Ubuntu Storybook CI corrective（`a1efe439`後）: Ubuntu runnerに`fonts-noto-color-emoji`をCI、release preflight、release workflowの全acceptance pathで明示導入し、公開済みKUC `v0.3.5` tag由来の`93cdc4ee9aa40e2afceecc63da0ca05ec7aab4bec991ece51a6b52389f48a477`と`NotoColorEmoji.ttf`のSHA-256を照合して、compile前に`KUC_PINNED_LINUX_EMOJI_SHA256`をexportする。KatanA fixtureのemoji chromatic pixelとemojiを含むheadingの選択/copy boundsをOS font依存の欠落なしに検証する。5件のregression、three-OS CI、full release gateを最終HEADで通すまでReady化しない。証跡: command: GitHub Actions run <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/33859162601/job/100979349391>; KUC release <https://github.com/HiroyukiFuruno/katana-ui-core/releases/tag/v0.3.5>. delegation-exception: `直列のクリティカルパス`.
- [/] PR #50 fresh-head P1/P2 review follow-up（`a1efe439`後）: Office profiling parserは`session`/`source`相関フィールドを許容し、`dateGroupItem`をtyped semantics実装までunsupported AutoFilter criterionとして扱う。viewer link targetは正確な`a`要素の`href`属性だけを受理し、direct HTML CSSの引用符入り値は有効なstyle attributeへescapeする。各回帰、full release gate、4 threadのreply/resolve、さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 review threads.
- [/] PR #50 fresh-head P2 review follow-up（`d51700c`後）: inline HTML CSSの最後の有効な`font-weight`と`text-decoration`宣言を三値overrideとして保持し、`normal`/`none`が親または先行宣言のbold、underline、strikethroughを明示解除できるようにする。両renderer経路の親継承解除と同一attribute内の後勝ち回帰を維持し、full release gate、thread reply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib css_ --locked`; PR #50 thread `PRRT_kwDOSTfBFs6fRJyC`.
- [/] PR #50 fresh-head P2 review follow-up（`52577a7`後）: HTMLの`href`は属性名完全一致かつ単引用符を含む有効な引用符値だけを受理し、単引用符属性内の`>`でtokenizerを終端しない。Windows Office workerはDEBUG時だけstderrを親traceへ継承し、Windows XLSX parent spawnも`spreadsheet.worker_spawn`を計測する。platform別static verifierと回帰、full release gate、4 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy python3 scripts/feasibility/verify-office-profiling-stages.py`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6ffvZW`, `PRRT_kwDOSTfBFs6ffvZX`, `PRRT_kwDOSTfBFs6ffvZZ`, `PRRT_kwDOSTfBFs6ffvZb`.
- [/] PR #50 CI V8 cache corrective（`52577a7`後）: three-OS CI、release preflight、release workflowはRust cache復元直後に`cargo clean -p v8`でV8 build-script成果物だけを再生成対象に戻し、`rusty_v8` static archiveを正規取得・検証してlinkする。`RUSTY_V8_SKIP_DOWNLOAD`、依存downgrade、test無効化、cache無効化は禁止。全OS CIとrelease checkを最終HEADで通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo clean -p v8`; `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy python3 scripts/release/verify-release-contract.py --target-version v0.5.6`; PR #50 Actions runs.
- [/] PR #50 fresh-head P2 follow-up（`a7edfa5`後）: unquoted root-relative/protocol-relative `href`の先頭slashを保持し、export surfaceの`br`をstyle/link context付きnewline spanへ変換する。SpreadsheetMLの`row.hidden`は手動非表示と保存済みAutoFilterで共通のため、再生可能な保存条件と一致するsource hiddenをfilter layerへ移しClearで復元し、条件外のsource hiddenだけをauthored layerに保持する。重複起源はファイル上で表現されないため保存済みfilterを優先する。各回帰、full release gate、3 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy cargo test -p katana-document-viewer --lib persisted_filter --locked`; `rtk proxy cargo test -p katana-document-viewer --lib opening_persisted_auto_filter_keeps_criteria_and_clear_restores_filtered_rows --locked`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6fgkfo`, `PRRT_kwDOSTfBFs6fgkfp`, `PRRT_kwDOSTfBFs6fgkfq`.
- [/] PR #50 fresh-head P1/P2 follow-up（`725cd18`後）: 保存済みAutoFilterのserialized `row.hidden`をfilter layerへ移管してClearで復元し、条件外のsource hiddenを保持する。生成image URIのpath bytesとMarkdown alt delimiterをescapeし、`font-style: normal`を両rendererで三値overrideとして親italicから明示解除する。CSS tag selectorのASCII case-insensitive性を維持したままclass/id値はcase-sensitiveにする。各回帰、full release gate、4 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib persisted_filter --locked`; `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy cargo test -p katana-document-viewer --lib direct_html_css --locked`; `rtk proxy cargo test -p katana-document-viewer --lib source_normalizer --locked`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6fg6GT`, `PRRT_kwDOSTfBFs6fg6GW`, `PRRT_kwDOSTfBFs6fg6GX`, `PRRT_kwDOSTfBFs6fg6Gb`.
- [/] PR #50 fresh-head P2 follow-up（`dd00ade`後）: Viewer node-planは全HTML void要素をcontext-freeとして扱い、slashなしのvoid tagから後続兄弟textへinline style/link contextが漏れないようにする。Windows AppContainer XLSX workerはprotocolのstdin/stdout pipeを維持したままstderrを連続drainし、DEBUG traceだけを親stderrへ転送する。各回帰、platform static verifier、full release gate、2 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy cargo test -p katana-document-viewer --lib spreadsheet_worker_spawn --locked`; `rtk proxy python3 scripts/feasibility/verify-office-profiling-stages.py`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6fhS8J`, `PRRT_kwDOSTfBFs6fhS8L`.
- [/] PR #50 platform gate corrective（`dd00ade`後）: llvm-cov固有の`target/llvm-cov-target`でV8を再生成してcache由来の`rusty_v8`欠落を防ぎ、Windows Office workerの`StdioConfig`契約を`PartialEq`不要のvariant判定で検証する。Linux preview cropが失敗した場合はthreshold/referenceを変えずcandidate/reference dumpをartifact化して原因を判別する。各platform test、release contract、full release gate、最終HEADのrequired CIを通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy just coverage`; `rtk proxy python3 scripts/release/verify-release-contract.py --self-test`; GitHub Actions exact final SHA.
- [/] PR #50 fresh-head P1 follow-up（`237928b`後）: Windows DEBUG時のXLSX worker stderr relayはworker EOFまで親stderr lockを保持せず、readした各chunkのwrite中だけlockを取る。worker protocol stdin/stdoutとstderr drainを維持し、chunk境界でlockが解放される回帰・platform static verifier・full release gate・thread reply/resolve・最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. PR #50 thread `PRRT_kwDOSTfBFs6fh2hJ`. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib dependency_tests --locked`; `rtk proxy cargo test -p katana-document-viewer --lib spreadsheet_worker_spawn --locked`; `rtk proxy just ast-lint`.
- [/] PR #50 fresh-head P2 follow-up（`ae841891`後）: relative direct-image referenceはsource directoryから有効なlocal file URIへ解決してdiagram fallbackへ流さず、viewer/export双方のrich HTML spanは連続するblock要素のtext境界を保持する。各回帰、full release gate、2 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. PR #50 threads `PRRT_kwDOSTfBFs6flcbu`, `PRRT_kwDOSTfBFs6flcbx`. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib source_normalizer --locked`; `rtk proxy cargo test -p katana-document-viewer --lib asset_loader --locked`; `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy just VERSION=v0.5.6 release-check`.
- [/] PR #50 patch API compatibility follow-up: v0.5.5で構築可能だった`SpreadsheetCellStyleArtifact`、`SpreadsheetSheetArtifact`、`DocumentFrame`、`DocumentGridCellAppearance`のstruct literalと既存enum exhaustivenessをv0.5.6で維持し、新しいborder/AutoFilter/frame metadataをprivate sidecarまたは追加methodへ分離する。`cargo-semver-checks`と外部consumer形状のcompile regressionをrelease gateへ追加し、P1 threadへのreply/resolve、最終HEAD review、required CIを通すまでReady化しない。PR #50 thread `PRRT_kwDOSTfBFs6kBYHl`. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy just semver-check`; `rtk proxy cargo test -p katana-document-viewer --test public_patch_api_compatibility --no-run --locked`.
- [/] Issue #51: KDVの公開`ViewerTableProjection`からStorybook export surfaceのKUC `GenericGrid`を構築し、Markdown/HTML tableのrow/cell、alignment、wrap、column/row geometryを保持する。diagram exportはimageを明示的なtop/bottom margin付きwrapperへ分離し、offset許容ではなくwrapper/image geometryの直接contractで固定する。reference、閾値、scale、path/git overrideは変更せず、focused regression、full release gate、Issue更新、最終HEAD reviewを通す。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/51>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p kdv-storybook table_node_preserves_rows_cells_alignment_wrapping_and_geometry --locked`; `rtk proxy cargo test -p kdv-storybook explicit_diagram_wrapper_geometry --locked`.
- [x] 6.3 Issue #49: normalize DEBUG-only first-frame trace stages for archive intake, worker spawn, runtime init, package parse, transfer, conversion, parse/layout, frame publication, and raster across static Office and XLSX workers without changing limits or cache lifecycle. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49> / 証跡: file: `evidence/issue-coverage.md`.
  - [x] Windows AppContainer経路でも `office.worker_spawn` をDEBUG-onlyで計測し、profiling stage verifierにplatform別のsource契約を追加する。delegation-exception: `直列のクリティカルパス` / 証跡: `scripts/feasibility/verify-office-profiling-stages.py`.
- [x] 6.4 Issue #49: run the supplied PPTX corpus through repeated cold first-frame measurement and record p50/p95, RSS delta, dominant stage, unchanged-source reuse, and ten-cycle cleanup evidence. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49> / 証跡: file: `evidence/pptx-performance.md`.
  - [x] Add a cold-process PPTX measurement harness that requires stage traces and records per-run/p50/p95/RSS evidence without substituting a generated fixture. delegation-exception: `直列のクリティカルパス`
  - [x] Execute the harness against the three supplied PPTX files after their source paths or attachments are available. delegation-exception: `直列のクリティカルパス`
- [x] 6.5 Issue #48: LibreOffice 26.8.0.3 / 72 dpi source renderer、fixture hash、viewport、DOCX/XLSX baseline/candidate scoreを固定し、KDVのXLSX border metadataをworkerから公開frameまで保持する。KatanA固有補正は行わない。delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48> / 証跡: file: `evidence/fidelity-reference.json`, `evidence/fidelity-baseline.json`, `evidence/issue-coverage.md`.
- [x] 6.8 Issue #48: KDV はKUC `0.3.5`で公開されたper-side style/color border型をthin projectionへ接続し、同一fidelity harnessの実測で`custom border=true`、`border_visual_missing_count=0`を確認した。現行候補はregistry KUC `0.3.6`を採用し、公開後KatanA acceptanceでhost側referenceを再生成する。locked KDV全体ゲートも5.3のローカル範囲で通過済みだが、公開リリースとKatanA採用は別DoDとして残る。delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48> / 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy env PATH=/Applications/LibreOffice.app/Contents/MacOS:$PATH python3 scripts/feasibility/measure-office-fidelity.py --verify-record openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/fidelity-baseline.json`.
- [x] 6.6 Enforce the KRR 0.4.20 / KDV V8 singleton locally with locked manifest/lockfile checks, `cargo tree -d`, inverse-tree ownership, and a public consumer link test. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `evidence/dependency-maintenance.md`.
- [ ] 6.7 After publishing, build and link a fresh crates.io-only consumer of the exact KDV version; it must resolve KDV and V8 from registry without path/git overrides or duplicate V8. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `evidence/issue-coverage.md`.
  - [x] Consumer verifierはroot以外のpath/git sourceをすべて拒否し、公開後build前にregistryのexact KDV/KRR/V8 packageを必須化する。delegation-exception: `直列のクリティカルパス` / 証跡: `scripts/release/verify-registry-consumer-link.py --self-test`.
- [x] FB-2026-08-29-002: KatanAの7.7 KiB XLSX cold/warm差（初回open 3,820 ms / frame 3,857 ms、warm open 14–16 ms / frame 42–45 ms、steady RSS +912 KiB）を#49のstage profilingへ反映し、既存v0.5.6 gateを下げない。delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49#issuecomment-5461739569> / 証跡: file: `evidence/pptx-performance.md`.
