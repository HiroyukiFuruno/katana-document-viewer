## 1. Reproduction and ownership evidence

delegation-exception: `直列のクリティカルパス` / file:
`openspec/changes/post-v0-5-5-document-fidelity-regressions/handoff.md`

- [x] 1.1 Add a real data-descriptor DOCX/XLSX/PPTX fixture and record the exact local-header/central-directory behavior. delegation-exception: `直列のクリティカルパス`
- [x] 1.2 Run the supplied XLSX/PPTX corpus through KDV and record typed failures and stage-level first-frame timings. delegation-exception: `直列のクリティカルパス`
- [x] 1.3 Record child-process, cache-entry, artifact-byte, and process-count baselines for ten mixed-document open/frame/close cycles. delegation-exception: `直列のクリティカルパス`

## 2. Office ZIP compatibility

- [x] 2.1 Replace string-matched deferred-length handling with a typed local-header scan outcome. delegation-exception: `直列のクリティカルパス`
- [x] 2.2 Accept valid data-descriptor/ZIP64 packages only after central-directory, CRC, duplicate, path, relationship, and resource-limit validation. delegation-exception: `直列のクリティカルパス`
- [x] 2.3 Add regression tests proving valid variants pass and corrupt or unsafe variants remain rejected before process spawn. delegation-exception: `直列のクリティカルパス`

## 3. Interactive spreadsheet filtering

- [x] 3.1 Add neutral AutoFilter range, column, criterion, candidate, visibility, and diagnostic artifact types. delegation-exception: `直列のクリティカルパス`
- [x] 3.2 Parse bounded worksheet AutoFilter metadata for model and streaming spreadsheet backends. delegation-exception: `直列のクリティカルパス`
- [x] 3.3 Add isolated-process requests/responses for candidate extraction and filter evaluation without materializing the full sheet into host IPC. delegation-exception: `直列のクリティカルパス`
- [x] 3.4 Add KDV document commands/events and rebuild grid row visibility while preserving source row indices, selection, scroll, frozen panes, and authored hidden rows. delegation-exception: `直列のクリティカルパス`
- [x] 3.5 Add unit, protocol, session, and rendered-frame tests for apply, clear, unsupported criteria, and large-sheet limits. delegation-exception: `直列のクリティカルパス`

## 4. Performance and lifecycle

- [x] 4.1 Add `DEBUG=true`-only stage tracing for preflight, spawn, convert, decode, frame, cache, close, and drop. delegation-exception: `直列のクリティカルパス`
- [x] 4.2 Key immutable Office conversion artifacts by content, format, and process settings and prevent navigation, resize, or repeat-frame reconversion. delegation-exception: `直列のクリティカルパス`
- [x] 4.3 Bound page/grid/artifact caches by entries and bytes with deterministic eviction tests. delegation-exception: `直列のクリティカルパス`
- [x] 4.4 Make close/drop cleanup idempotent and prove process, workspace, frame, and cache live counts return to baseline across PDF, XLSX, and PPTX ten-cycle sessions. delegation-exception: `直列のクリティカルパス`
- [x] 4.5 Measure the supplied PPTX corpus, remove the dominant avoidable stage, and record the before/after first-frame result. delegation-exception: `直列のクリティカルパス`

## 5. Verification, dependency maintenance, and release

- [x] 5.1 Run focused ZIP, spreadsheet filter, performance, and lifecycle regression suites. delegation-exception: `直列のクリティカルパス`
- [x] 5.2 Update compatible direct/transitive dependencies and lockfile, including public KRR `0.4.19` with caret-compatible semver (not an exact pin), without lowering lint, coverage, security, score, or acceptance gates. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `evidence/dependency-maintenance.md`.
  - [x] KRR and every non-KUC dependency are updated or rechecked against the registry; the lockfile updates compatible `libredox`、`rust_decimal`、`smallvec` and needs no further compatible update. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just update`
  - [x] KDV manifest and release-contract checks pin registry-only `katana-ui-core` `0.3.6` with no path/git override; KUC `v0.3.6` tag, GitHub Release, crates.io publication, and a fresh exact registry consumer build were verified. `just update` refreshed all compatible non-KUC dependencies without lowering a gate. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just update`
  - [x] 2026-09-06にKUC `v0.3.7`のtag、GitHub Release、crates.io artifactを確認し、KDVはexact registry `=0.3.7`へ更新した。manifest/lockfileのsource+checksum、release contract、document-surface boundary、DoD self-test、V8 singleton、依存契約・score gate契約テスト、format、AST lint、strict clippyを通過し、path/git overrideやV8重複を導入していない。公開KDV consumerとKatanA acceptanceは別DoDとして残す。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`.
  - [x] The former development-only KUC Storybook Git/tag dependency was replaced by the public registry package. Registry Storybook smoke passed with `143/30/54/121` tests against KUC `0.3.6`; no final-release Git/tag override remains. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just storybook-kuc-smoke`
  - [x] The release verifier accepts every registry KRR resolution compatible with `^0.4.19` and rejects `0.5.0`; the V8 singleton/link checks passed with one resolved V8 runtime. 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy python3 scripts/release/verify-v8-runtime-singleton.py`
- [/] FB-2026-08-29-001: 全体のKUC blockerでも、KDV内で独立して進められる品質ゲート、Draft PR、CI準備を停止しない。後続の状況確認はこの実装指示を置換しないため、報告前に未完了項目を実行可能・外部入力待ち・公開待ちへ分け、実行可能な項目を継続する。グローバルポリシーの表記差は`reasoning`の`medium`基準を意味で検証し、回帰テストで維持する。delegation-exception: `直列のクリティカルパス` / 証跡: file: `scripts/check-subagent-spark-harness-policy-tests.sh`.
<!-- subagent-spark-harness-strict-start -->
- [ ] 5.3 Run strict OpenSpec validation, `just check`, 100% coverage, boundary, scorecard, package, publish dry-run, and three-OS CI gates. delegation-exception: `直列のクリティカルパス`
- [x] 2026-09-04の候補作業ツリーで`rtk proxy just VERSION=v0.5.6 release-check`はexit `0`。strict coverageはfunctions `3531/3531`・lines `29061/29061`（各100%）、data-descriptor DOCX worker、V8 singleton、package build、`cargo publish --dry-run`、未公開registry確認まで通過した。KatanA候補referenceの更新後にはPR最終HEADで同gateを再実行する。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just VERSION=v0.5.6 release-check`。delegation-exception: `直列のクリティカルパス`
  - [x] 2026-09-05の候補差分で`rtk proxy just VERSION=v0.5.6 release-check`はexit `0`。Retina図表スクロール境界の回帰、strict coverage functions `3532/3532`・lines `29064/29064`（各100%）、data-descriptor DOCX worker、V8 singleton、package build、`cargo publish --dry-run`、未公開registry確認まで通過した。three-OS CIとPR最終HEADのfresh reviewは未完了の公開gateとして残す。証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy just VERSION=v0.5.6 release-check`。delegation-exception: `直列のクリティカルパス`
  - [ ] three-OS CI は、同一差分を commit/push した Draft PR 上で実行する公開 gate。
- [/] KRR 0.4.19 のrenderer更新でKatanA canonical score artifactが陳腐化したため、95点gateの緩和またはKDV自己比較をせず、KDV v0.5.6候補を実際に採用したKatanAから独立生成したexport PNGとpreview cropを取得する。generator、fixture SHA-256、KDV/KRR解決版、出力SHA-256・寸法を記録して追跡済みreferenceを更新し、focused scoreとrelease gateを再実行する。KDV 0.5.5 / KRR 0.4.19で生成されたexport PNG（`2dddc1c54b07e3f1132ea0e269d2106e074b3864b1faafb734110939b3afe66d`、1280x19067）は陳腐化の診断証跡であり、v0.5.6の最終referenceには使わない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`. delegation-exception: `直列のクリティカルパス`
  - [/] 2026-09-06: clean package SHA-256 `3196397aec631fc31524d70647c7fdc12479be16698f955a09deb48bdca07020`のみをloopback sparse registryから解決した使い捨てKatanA cloneで、KDV `0.5.6`、KRR `0.4.19`、KUC `0.3.7`、V8 `152.2.0`の単一解決を確認した。実機runnerがfixture `sample.md` / `sample_diagrams.md`からexportとfull screenshotを生成したが、`sample_diagrams.md`の図表本体が未描画となり、既存の客観scoreは`3/95`だった。既存physical crop `2374x4450+64+202`をBox filterで`1280x2400`へ正規化して診断したが、追跡referenceは直ちに復元済みである。KatanA host output SHA-256はexport `88a3342dadc95e46fa1db5e5c942017670e399d8199eca63b57ff73622675a1c`、sample full `3ec7ab6e530ec68acce6655c881af858f6bd3a36a5437be52b2a0e5c970849de`、diagrams full `11cced597dacbe2e641fd850b49d6744721ec2e3dfd3e41fed175994d63f4321`。実機frame描画の回帰を修正してfocused scoreとrelease gateを再実行するまで未完了。証跡: issue: <https://github.com/HiroyukiFuruno/KatanA/issues/339#issuecomment-5556255336>; file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md`. delegation-exception: `直列のクリティカルパス`
- [/] Preflight candidate-registry lane: cleanなKDV release commitを`cargo package --locked`し、SHA-256を持つ`.crate`だけを127.0.0.1 loopback sparse registryで配信する。使い捨てKatanA cloneはexact `=0.5.6`をそのregistryから解決し、KDV依存へのpath/git/patch overrideなし・KDV/KRR/V8単一解決をmetadata/lockfileで証明したうえでhost export/cropを生成する。このlaneはpreflight循環を断つ候補証跡に限り、公開後にはfresh crates.io KDV artifactのchecksum比較、registry-only consumer link、KatanA packaged acceptanceを別途必須とする。fixture/request/crop/normalization/PNG/hash/log/versionを記録し、推測でreferenceを更新しない。Issue: <https://github.com/HiroyukiFuruno/katana/issues/339>. delegation-exception: `直列のクリティカルパス`.
  - [x] loopback sparse registry builderは、128 MiB crate、50,000 member、512 MiB unpacked、1 MiB `Cargo.toml`の上限、symlink/path/git/registry override拒否、型付きCargo metadata、同一filesystemのtemporary→rename publishを実装し、失敗時にpartial outputを残さない。malformed gzip、manifest欠落、invalid TOML、unsafe archive entry、型不正、非registry依存をself-testで拒否する。証跡: command: `rtk proxy python3 -B scripts/feasibility/build_candidate_registry.py --self-test`; file: `scripts/feasibility/build_candidate_registry.py`. delegation-exception: `直列のクリティカルパス`.
- [ ] 5.4 Publish the KDV patch release and verify the GitHub Release and crates.io artifact. delegation-exception: `直列のクリティカルパス`
- [ ] 5.5 Update KatanA to the exact published registry version with no path/git override and rerun the supplied-file and ten-cycle packaged acceptance suites. delegation-exception: `直列のクリティカルパス`

## 6. v0.5.6 issue coverage added after owner-layer audit

- [x] 6.1 Issue #46: add the exact KatanA data-descriptor DOCX SHA-256 fixture, verify all 20 local headers defer CRC/sizes, and prove the isolated worker generates a frame without lowering archive limits. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/46>.
  証跡: `evidence/issue-coverage.md`.
- [x] 6.2 Issue #47: cover typed string, numeric, blank, multiple-value, and clear AutoFilter behavior from a real XLSX-derived fixture while preserving grid frame visibility. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>.
  証跡: `evidence/issue-coverage.md`.
- [/] PR #50 P1 follow-up: persist supported saved AutoFilter criteria at open, distinguish their hidden rows from authored hidden rows when clearing, stream filter chunks without retaining the full grid, return current materialization responses independently of bounded cache admission, and tokenize HTML spans at inline-style boundaries. Run focused regressions, a full local release gate, reply/resolve every P1 thread, and obtain a fresh-head review before Ready. Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. 証跡: `rtk proxy just coverage`; `rtk proxy just JOBS=2 check`. delegation-exception: `直列のクリティカルパス`.
  証跡: `rtk proxy just coverage`; `rtk proxy just JOBS=2 check`.
- [/] PR #50 fresh-head P2 follow-up: apply/clear後のAutoFilter criteriaをartifact/frameへ同期し、候補値をworker response byte上限内でtruncateし、direct HTML CSS attribute名を完全一致で判定し、SpreadsheetCellArtifactが所有する全heap領域をcache容量へ算入する。各回帰・full release gate・4 threadのreply/resolve・さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. 証跡: `rtk proxy cargo test -p katana-document-viewer document_session_spreadsheet --lib`; `rtk proxy cargo test -p katana-document-viewer spreadsheet_cell_cache --lib`; `rtk proxy cargo test -p katana-document-viewer direct_html_css --lib`; `rtk proxy just VERSION=v0.5.6 release-check`. delegation-exception: `直列のクリティカルパス`.
  証跡: `rtk proxy cargo test -p katana-document-viewer document_session_spreadsheet --lib`; `rtk proxy cargo test -p katana-document-viewer spreadsheet_cell_cache --lib`; `rtk proxy cargo test -p katana-document-viewer direct_html_css --lib`; `rtk proxy just VERSION=v0.5.6 release-check`.
- [/] PR #50 fresh-head P1/P2 review follow-up: Viewer node-planでrich HTMLのstyle境界と`br`境界を保持し、同一行の全HTML要素へCSSを独立適用し、DEBUG Office/XLSX trace全行をsession/source識別子で関連付ける。各回帰・full release gate・4 threadのreply/resolve・さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib`; `rtk proxy cargo clippy -p katana-document-viewer --lib --tests -- -D warnings`.
  証跡: `rtk proxy cargo test -p katana-document-viewer --lib`; `rtk proxy cargo clippy -p katana-document-viewer --lib --tests -- -D warnings`; PR #50 review threads.
- [/] PR #50 fresh-head P1/P2 follow-up（`050395a`後）: streaming backendの候補抽出・filter評価は各operationでworksheet ZIP entryを一回だけ逐次走査し、bounded chunkと既存filter semanticsを保持する。候補responseは後続の別request IDの`ApplyFilter` JSON（改行込み）でも512 KiB request上限内に収める。large streaming XLSXの一回走査/候補/適用/clearと最大request ID境界を回帰化し、full release gate、2 threadのreply/resolve、さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib spreadsheet_engine::filter_tests`; `rtk proxy cargo test -p katana-document-viewer --lib candidate_truncation`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 review threads.
- [/] PR #50 CI corrective: Ubuntu/Windows workspace testでbrowser sessionの初期frame生成が失敗した根因を修正し、初期frame・owner停止後closeの実経路を回帰で固定する。timeout延長・test無効化・品質ゲート低下は禁止。根因はKRRの並行V8 cold-start契約であり、owner issue <https://github.com/HiroyukiFuruno/katana-render-runtime/issues/76> の公開registry修正版採用後に対象CIとfull release gateを最終HEADで通すまでReady化しない。delegation-exception: `書き込み範囲を明示できない`. 証跡: GitHub Actions run `33840894570` / jobs `100922795136`, `100922794777`; issue: <https://github.com/HiroyukiFuruno/katana-render-runtime/issues/76>.
  証跡: GitHub Actions run `33840894570` / jobs `100922795136`, `100922794777`; KRR issue <https://github.com/HiroyukiFuruno/katana-render-runtime/issues/76>.
- [/] PR #50 Ubuntu Storybook CI corrective（`a1efe439`後）: Ubuntu runnerに`fonts-noto-color-emoji`をCI、release preflight、release workflowの全acceptance pathで明示導入し、公開済みKUC `v0.3.5` tag由来の`93cdc4ee9aa40e2afceecc63da0ca05ec7aab4bec991ece51a6b52389f48a477`と`NotoColorEmoji.ttf`のSHA-256を照合して、compile前に`KUC_PINNED_LINUX_EMOJI_SHA256`をexportする。KatanA fixtureのemoji chromatic pixelとemojiを含むheadingの選択/copy boundsをOS font依存の欠落なしに検証する。5件のregression、three-OS CI、full release gateを最終HEADで通すまでReady化しない。証跡: command: GitHub Actions run <https://github.com/HiroyukiFuruno/katana-document-viewer/actions/runs/33859162601/job/100979349391>; KUC release <https://github.com/HiroyukiFuruno/katana-ui-core/releases/tag/v0.3.5>. delegation-exception: `直列のクリティカルパス`.
- [/] PR #50 fresh-head P1/P2 review follow-up（`a1efe439`後）: Office profiling parserは`session`/`source`相関フィールドを許容し、`dateGroupItem`をtyped semantics実装までunsupported AutoFilter criterionとして扱う。viewer link targetは正確な`a`要素の`href`属性だけを受理し、direct HTML CSSの引用符入り値は有効なstyle attributeへescapeする。各回帰、full release gate、4 threadのreply/resolve、さらにfresh-head reviewを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 review threads.
- [/] PR #50 fresh-head P2 review follow-up（`d51700c`後）: inline HTML CSSの最後の有効な`font-weight`と`text-decoration`宣言を三値overrideとして保持し、`normal`/`none`が親または先行宣言のbold、underline、strikethroughを明示解除できるようにする。両renderer経路の親継承解除と同一attribute内の後勝ち回帰を維持し、full release gate、thread reply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib css_ --locked`; PR #50 thread `PRRT_kwDOSTfBFs6fRJyC`.
- [/] PR #50 fresh-head P2 review follow-up（`52577a7`後）: HTMLの`href`は属性名完全一致かつ単引用符を含む有効な引用符値だけを受理し、単引用符属性内の`>`でtokenizerを終端しない。Windows Office workerはDEBUG時だけstderrを親traceへ継承し、Windows XLSX parent spawnも`spreadsheet.worker_spawn`を計測する。platform別static verifierと回帰、full release gate、4 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy python3 scripts/feasibility/verify-office-profiling-stages.py`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6ffvZW`, `PRRT_kwDOSTfBFs6ffvZX`, `PRRT_kwDOSTfBFs6ffvZZ`, `PRRT_kwDOSTfBFs6ffvZb`.
- [/] PR #50 CI V8 cache corrective（`52577a7`後）: three-OS CI、release preflight、release workflowはRust cache復元直後に`cargo clean -p v8`でV8 build-script成果物だけを再生成対象に戻し、`rusty_v8` static archiveを正規取得・検証してlinkする。`RUSTY_V8_SKIP_DOWNLOAD`、依存downgrade、test無効化、cache無効化は禁止。全OS CIとrelease checkを最終HEADで通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo clean -p v8`; `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy python3 scripts/release/verify-release-contract.py --target-version v0.5.6`; PR #50 Actions runs.
- [/] PR #50 fresh-head P2 follow-up（`a7edfa5`後）: unquoted root-relative/protocol-relative `href`の先頭slashを保持し、export surfaceの`br`をstyle/link context付きnewline spanへ変換する。SpreadsheetMLの`row.hidden`は手動非表示と保存済みAutoFilterで共通のため、再生可能な保存条件と一致するsource hiddenをfilter layerへ移しClearで復元し、条件外のsource hiddenだけをauthored layerに保持する。重複起源はファイル上で表現されないため保存済みfilterを優先する。各回帰、full release gate、3 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy cargo test -p katana-document-viewer --lib persisted_filter --locked`; `rtk proxy cargo test -p katana-document-viewer --lib opening_persisted_auto_filter_keeps_criteria_and_clear_restores_filtered_rows --locked`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6fgkfo`, `PRRT_kwDOSTfBFs6fgkfp`, `PRRT_kwDOSTfBFs6fgkfq`.
- [/] PR #50 fresh-head P1/P2 follow-up（`725cd18`後）: 保存済みAutoFilterのserialized `row.hidden`をfilter layerへ移管してClearで復元し、条件外のsource hiddenを保持する。生成image URIのpath bytesとMarkdown alt delimiterをescapeし、`font-style: normal`を両rendererで三値overrideとして親italicから明示解除する。CSS tag selectorのASCII case-insensitive性を維持したままclass/id値はcase-sensitiveにする。各回帰、full release gate、4 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/47>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib persisted_filter --locked`; `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy cargo test -p katana-document-viewer --lib direct_html_css --locked`; `rtk proxy cargo test -p katana-document-viewer --lib source_normalizer --locked`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6fg6GT`, `PRRT_kwDOSTfBFs6fg6GW`, `PRRT_kwDOSTfBFs6fg6GX`, `PRRT_kwDOSTfBFs6fg6Gb`.
- [/] PR #50 fresh-head P2 follow-up（`dd00ade`後）: Viewer node-planは全HTML void要素をcontext-freeとして扱い、slashなしのvoid tagから後続兄弟textへinline style/link contextが漏れないようにする。Windows AppContainer XLSX workerはprotocolのstdin/stdout pipeを維持したままstderrを連続drainし、DEBUG traceだけを親stderrへ転送する。各回帰、platform static verifier、full release gate、2 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>, <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy cargo test -p katana-document-viewer --lib spreadsheet_worker_spawn --locked`; `rtk proxy python3 scripts/feasibility/verify-office-profiling-stages.py`; `rtk proxy just VERSION=v0.5.6 release-check`; PR #50 threads `PRRT_kwDOSTfBFs6fhS8J`, `PRRT_kwDOSTfBFs6fhS8L`.
- [/] PR #50 platform gate corrective（`dd00ade`後）: llvm-cov固有の`target/llvm-cov-target`でV8を再生成してcache由来の`rusty_v8`欠落を防ぎ、Windows Office workerの`StdioConfig`契約を`PartialEq`不要のvariant判定で検証する。Linux preview cropが失敗した場合はthreshold/referenceを変えずcandidate/reference dumpをartifact化して原因を判別する。各platform test、release contract、full release gate、最終HEADのrequired CIを通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy just coverage`; `rtk proxy python3 scripts/release/verify-release-contract.py --self-test`; GitHub Actions exact final SHA.
- [/] PR #50 fresh-head P1 follow-up（`237928b`後）: Windows DEBUG時のXLSX worker stderr relayはworker EOFまで親stderr lockを保持せず、readした各chunkのwrite中だけlockを取る。worker protocol stdin/stdoutとstderr drainを維持し、chunk境界でlockが解放される回帰・platform static verifier・full release gate・thread reply/resolve・最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issues: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49>. PR #50 thread `PRRT_kwDOSTfBFs6fh2hJ`. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib dependency_tests --locked`; `rtk proxy cargo test -p katana-document-viewer --lib spreadsheet_worker_spawn --locked`; `rtk proxy just ast-lint`.
- [/] PR #50 fresh-head P2 follow-up（`ae841891`後）: relative direct-image referenceはsource directoryから有効なlocal file URIへ解決してdiagram fallbackへ流さず、viewer/export双方のrich HTML spanは連続するblock要素のtext境界を保持する。各回帰、full release gate、2 threadのreply/resolve、さらに最終HEADのfresh reviewとrequired CIを通すまでReady化しない。Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48>. PR #50 threads `PRRT_kwDOSTfBFs6flcbu`, `PRRT_kwDOSTfBFs6flcbx`. delegation-exception: `直列のクリティカルパス`. 証跡: `rtk proxy cargo test -p katana-document-viewer --lib source_normalizer --locked`; `rtk proxy cargo test -p katana-document-viewer --lib asset_loader --locked`; `rtk proxy cargo test -p katana-document-viewer --lib html_spans --locked`; `rtk proxy just VERSION=v0.5.6 release-check`.
- [x] 6.3 Issue #49: normalize DEBUG-only first-frame trace stages for archive intake, worker spawn, runtime init, package parse, transfer, conversion, parse/layout, frame publication, and raster across static Office and XLSX workers without changing limits or cache lifecycle. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49> / 証跡: file: `evidence/issue-coverage.md`.
  - [x] Windows AppContainer経路でも `office.worker_spawn` をDEBUG-onlyで計測し、profiling stage verifierにplatform別のsource契約を追加する。delegation-exception: `直列のクリティカルパス` / 証跡: `scripts/feasibility/verify-office-profiling-stages.py`.
- [x] 6.4 Issue #49: run the supplied PPTX corpus through repeated cold first-frame measurement and record p50/p95, RSS delta, dominant stage, unchanged-source reuse, and ten-cycle cleanup evidence. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49> / 証跡: file: `evidence/pptx-performance.md`.
  - [x] Add a cold-process PPTX measurement harness that requires stage traces and records per-run/p50/p95/RSS evidence without substituting a generated fixture. delegation-exception: `直列のクリティカルパス`
  - [x] Execute the harness against the three supplied PPTX files after their source paths or attachments are available. delegation-exception: `直列のクリティカルパス`
- [x] 6.5 Issue #48: LibreOffice 26.8.0.3 / 72 dpi source renderer、fixture hash、viewport、DOCX/XLSX baseline/candidate scoreを固定し、KDVのXLSX border metadataをworkerから公開frameまで保持する。KatanA固有補正は行わない。delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48> / 証跡: file: `evidence/fidelity-reference.json`, `evidence/fidelity-baseline.json`, `evidence/issue-coverage.md`.
- [x] 6.8 Issue #48: KDV はKUC `0.3.5`で公開されたper-side style/color border型をthin projectionへ接続し、同一fidelity harnessの実測で`custom border=true`、`border_visual_missing_count=0`を確認した。現行候補はregistry KUC `0.3.6`を採用し、公開後KatanA acceptanceでhost側referenceを再生成する。locked KDV全体ゲートも5.3のローカル範囲で通過済みだが、公開リリースとKatanA採用は別DoDとして残る。delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/48> / 証跡: file: `openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/dependency-maintenance.md` / verify: `rtk proxy env PATH=/Applications/LibreOffice.app/Contents/MacOS:$PATH python3 scripts/feasibility/measure-office-fidelity.py --verify-record openspec/changes/post-v0-5-5-document-fidelity-regressions/evidence/fidelity-baseline.json`.
- [x] 6.6 Enforce the KRR 0.4.19 / KDV V8 singleton locally with locked manifest/lockfile checks, `cargo tree -d`, inverse-tree ownership, and a public consumer link test. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `evidence/dependency-maintenance.md`.
- [ ] 6.7 After publishing, build and link a fresh crates.io-only consumer of the exact KDV version; it must resolve KDV and V8 from registry without path/git overrides or duplicate V8. delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/44> / 証跡: file: `evidence/issue-coverage.md`.
  - [x] Consumer verifierはroot以外のpath/git sourceをすべて拒否し、公開後build前にregistryのexact KDV/KRR/V8 packageを必須化する。delegation-exception: `直列のクリティカルパス` / 証跡: `scripts/release/verify-registry-consumer-link.py --self-test`.
- [x] FB-2026-08-29-002: KatanAの7.7 KiB XLSX cold/warm差（初回open 3,820 ms / frame 3,857 ms、warm open 14–16 ms / frame 42–45 ms、steady RSS +912 KiB）を#49のstage profilingへ反映し、既存v0.5.6 gateを下げない。delegation-exception: `直列のクリティカルパス` / Issue: <https://github.com/HiroyukiFuruno/katana-document-viewer/issues/49#issuecomment-5461739569> / 証跡: file: `evidence/pptx-performance.md`.
